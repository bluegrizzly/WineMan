using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace WineMan
{
    public class RendezVous
    {
        public DateTime PickBestAvailableBottlingDate(DateTime startingDate)
        {
            DateTime result= startingDate;
            // Look in the transactions 
            return result;
        }

        public List<Appointement> GetAppointements(DateTime selectedDate)
        {
            List<Appointement> allApp = new List<Appointement>();

            return allApp;
        }
    }
}